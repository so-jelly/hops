name: Release

on:
  push:
    tags: ["[0-9]*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - run: swift test

      - name: Build universal binary
        run: swift build -c release --arch arm64 --arch x86_64

      - name: Assemble app bundle and zip
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: |
          sed -i '' "s/<string>1.0<\/string>/<string>${TAG_NAME}<\/string>/g" Info.plist
          mkdir -p hops.app/Contents/MacOS hops.app/Contents/Resources
          cp .build/apple/Products/Release/hops hops.app/Contents/MacOS/
          cp Info.plist hops.app/Contents/
          cp AppIcon.icns hops.app/Contents/Resources/
          zip -r "hops-${TAG_NAME}.zip" hops.app

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: hops-*.zip
