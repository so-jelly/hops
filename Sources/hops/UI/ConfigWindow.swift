import SwiftUI
import HopsCore

struct ConfigWindow: View {
    var body: some View {
        VStack {
            Text("hops")
                .font(.title)
            Text("Config UI coming next...")
        }
        .frame(width: 500, height: 400)
    }
}
